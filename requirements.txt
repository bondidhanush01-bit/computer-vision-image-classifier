# API-specific dependencies.
# The core ML dependencies (torch, opencv, ultralytics, mediapipe, etc.)
# come from the project root's requirements.txt — install that first:
#   pip install -r ../requirements.txt
#   pip install -r requirements.txt

fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.12
